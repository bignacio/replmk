prompt: "> "

initial_message: "Welcome to my custom REPL!"
input_note: "Type a command"
alt_help_cmd: "help"
alt_help_desc: "Show available commands"
alt_exit_cmd: "exit"
alt_exit_desc: "Exit the REPL."

# List of commands available in the REPL.
commands:
  - name: list
    description: "Lists files in the current directory. Usage: list [directory]"
    type: single
    exec: "ls"
  - name: environment
    description: "Lists all environment variables"
    type: single
    exec: "env"
  - name: example_output
    description: "Example using stdout and stderr"
    type: shell
    exec: |
      #!/bin/bash

      echo "received args $@"

      for arg in "$@"; do
        echo "argument: $arg"
      done
      sleep 2
      echo "running..."
      sleep 2
      echo "and this goes to stderr" 1>&2
      sleep 2
      echo "bye"
      sleep 2